title: A Case for Custom Types
--- |
  Before we define our own value `types` using `classes`, let's understand the problems we are trying to solve. Why would we ever define our own value `types`? Why not just keep using the ones that are built into Python?

  
